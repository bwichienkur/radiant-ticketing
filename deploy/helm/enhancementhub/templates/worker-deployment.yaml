apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "enhancementhub.fullname" . }}-worker
spec:
  replicas: {{ .Values.replicaCount.worker }}
  selector:
    matchLabels:
      app: {{ include "enhancementhub.fullname" . }}-worker
  template:
    metadata:
      labels:
        app: {{ include "enhancementhub.fullname" . }}-worker
    spec:
      containers:
        - name: worker
          image: "{{ .Values.image.repository }}-worker:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: {{ include "enhancementhub.fullname" . }}-config
          env:
            - name: ConnectionStrings__Default
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.externalDatabase.existingSecret }}
                  key: connectionString
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
          readinessProbe:
            httpGet: { path: /health/ready, port: 8080 }
          resources:
            {{- toYaml .Values.resources.worker | nindent 12 }}
